# Objection — O-xxx

Classification (pick exactly one): `STATIC_FACT` `MATHEMATICAL_DERIVATION`
`LOGICAL_ARGUMENT` `EMPIRICAL_HYPOTHESIS` `SPECULATIVE_CONCERN`
`NON_TESTABLE_CONCERN`

**`STATIC_FACT` requires a named, citable source** (textbook/paper/spec/
standard) in the Reasoning field below — an unsourced assertion does not
qualify; use `LOGICAL_ARGUMENT` (if you show the reasoning) or
`SPECULATIVE_CONCERN` (if you don't) instead. See `config.json` →
`static_fact_policy`.

- **Target claim:** `C-xxx`
- **Reviewer role:** code_auditor / math_auditor / scientific_auditor / red_team
- **Objection:** <the objection, stated precisely>
- **Why it could matter:** <impact if true>
- **Likelihood / relevance assessment:** <reviewer's honest estimate, and
  why — this is an opinion, label it as one>
- **Potential impact:** <magnitude/severity if confirmed>
- **Classification:** <one of the six above>
- **What evidence would support it:** <concrete observation>
- **What evidence would falsify it:** <concrete observation — answers "what
  would convince you this objection is wrong?">
- **Proposed experiment (if testable):** `E-xxx` or "not testable — see below"
- **Status:** OPEN / FALSIFIED / CONFIRMED / BOUNDED / NON-TESTABLE
- **Resolution (fill in when status changes from OPEN):** <what evidence,
  citing event/artifact, moved it here>
